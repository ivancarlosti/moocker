# Default port to expose the application locally.
MOODLE_PORT=8080

# The Public URL of your Moodle instance.
# If using a reverse proxy, set this to your domains URL (e.g. https://moodle.yourdomain.com)
MOODLE_URL=http://localhost:8080

# Force SSL proxy mode. Auto-detected from MOODLE_URL scheme by default.
# Set to "true" when behind a reverse proxy that terminates SSL (https:// wwwroot).
MOODLE_SSLPROXY=true

MOODLE_DATABASE_TYPE=mariadb
MOODLE_DATABASE_HOST=host.docker.internal
MOODLE_DATABASE_NAME=moodledb
MOODLE_DATABASE_USER=moodleuser
MOODLE_DATABASE_PASSWORD=moodlepass
